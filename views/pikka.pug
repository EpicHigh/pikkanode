extends layout
block content
    .container-pikka
        .title.block-image.w-100-ns.w-50-m.w-30-l.pikka-caption.shadow-3(style=`background-image: url("upload/${picture}")`)
            h2.post-title!= caption
            if (userId && userId.id)
                .tc!= likeButton
        if (commentBox)
            .comments.block-comment.w-100-ns.w-40-m.w-25-l.shadow-3!= commentBox
    if (userId && userId.id)
        .center.mw7.pt3.measure!= commentForm